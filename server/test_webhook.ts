import { handleIncomingMessage } from './src/services/conversations'; import { prisma } from './src/db'; async function test() { await handleIncomingMessage({ senderPsid: 'test_psid_123', recipientPageId: 'test_page_456', messageText: 'Hello from test', timestamp: new Date(), fbMessageId: 'mid.test.' + Date.now(), isEcho: false }); console.log('Done'); } test().then(() => prisma.$disconnect()).catch(console.error);
