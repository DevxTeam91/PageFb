import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function main() { const pages = await prisma.page.findMany(); console.log('Pages:', JSON.stringify(pages, null, 2)); const convs = await prisma.conversation.findMany({ take: 5, orderBy: { createdAt: 'desc' } }); console.log('Convs:', JSON.stringify(convs, null, 2)); } main().finally(() => prisma.$disconnect());
