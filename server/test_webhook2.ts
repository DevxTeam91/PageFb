import { handleIncomingMessage } from './src/services/conversations'; import { prisma } from './src/db'; async function test() { await handleIncomingMessage({ senderPsid: 'test_psid_999', recipientPageId: 'test_page_456', messageText: 'Hello from test 999', timestamp: new Date(), fbMessageId: 'mid.test.' + Date.now(), isEcho: false }); await new Promise(r => setTimeout(r, 2000)); console.log('Done'); } test().then(() => prisma.$disconnect()).catch(console.error);
